name: Automated Windows Virtual Workspace (ngrok-in)

on:
  workflow_dispatch:
    inputs:
      iso_url:
        description: "Direct ISO link for the OS you want to install"
        required: true
        default: ""

jobs:
  run-vm:
    runs-on: ubuntu-latest
    timeout-minutes: 360 # 6 hours

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🧩 Initializing Environment
        run: |
          echo "🔧 Preparing Your Virtual Workspace (4 Cores • 16 GB RAM • 256 GB SSD)"
          sudo apt update -qq
          # Added mtools and gpg
          sudo apt install -y qemu-system qemu-utils wget curl unzip mtools gpg
          mkdir -p $HOME/vm
          echo "✅ Environment initialized."

      - name: 💾 Downloading ISO
        run: |
          echo "📦 Downloading ISO from provided link..."
          cd $HOME/vm
          wget -O os.iso "${{ github.event.inputs.iso_url }}"
          ls -lh os.iso
          echo "✅ ISO downloaded successfully."

      - name: 💽 Creating 256 GB Virtual Disk
        run: |
          echo "💽 Creating 256 GB disk..."
          cd $HOME/vm
          qemu-img create -f qcow2 vm.qcow2 256G
          ls -lh vm.qcow2
          echo "✅ Disk created."

      - name: 💾 Creating Automated Install Floppy
        run: |
          echo "💿 Creating floppy disk image in $HOME/vm..."
          qemu-img create -f raw $HOME/vm/floppy.img 1.44M
          mkfs.vfat $HOME/vm/floppy.img
          
          echo "🤖 Copying .github/win/autounattend.xml from repo to floppy..."
          # Make sure your XML file is at this path in your repo
          mcopy -i $HOME/vm/floppy.img .github/win/autounattend.xml ::/autounattend.xml
          
          echo "✅ Floppy created with answer file."

      - name: 🌐 Setting Up ngrok (India Region)
        env:
          NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}
        run: |
          if [ -z "$NGROK_AUTH_TOKEN" ]; then
            echo "❌ Error: NGROK_AUTH_TOKEN secret is not set."
            echo "Please add your ngrok authtoken as a repository secret."
            exit 1
          fi
          echo "🔐 Installing ngrok..."
          curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | \
            sudo gpg --dearmor -o /usr/share/keyrings/ngrok-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/ngrok-archive-keyring.gpg] https://ngrok-agent.s3.amazonaws.com buster main" | \
            sudo tee /etc/apt/sources.list.d/ngrok.list
          sudo apt update -qq
          sudo apt install -y ngrok
          echo "🔑 Authenticating ngrok..."
          ngrok config add-authtoken $NGROK_AUTH_TOKEN
          echo "🔗 Starting ngrok tunnels in Indian (in) region..."
          nohup ngrok tcp --region in 5901 --log=stdout > $HOME/vm/ngrok-vnc.log &
          nohup ngrok tcp --region in 3389 --log=stdout > $HOME/vm/ngrok-rdp.log &
          echo "✅ ngrok setup done."

      - name: 🖥️ Launching VM
        run: |
          echo "🚀 Booting Virtual Machine..."
          cd $HOME/vm
          nohup sudo qemu-system-x86_64 \
            -enable-kvm \
            -cpu qemu64,kvm=on \
            -m 16G \
            -smp 4,sockets=1,cores=4,threads=1 \
            -boot order=dc \
            -vga std \
            -display vnc=:1 \
            -device qemu-xhci,id=xhci \
            -device usb-tablet,bus=xhci.0 \
            -netdev user,id=n0,hostfwd=tcp::3389-:3389 \
            -device e1000,netdev=n0,bootindex=-1 \
            \
            # --- Using VirtIO Disk + SATA CD ---
            -drive file=vm.qcow2,if=none,id=disk,format=qcow2 \
            -device virtio-blk-pci,drive=disk,bootindex=2 \
            -device ahci,id=sata \
            -drive file=os.iso,if=none,id=cdrom,media=cdrom,format=raw \
            -device ide-cd,bus=sata.0,drive=cdrom,bootindex=1 \
            -fda floppy.img \
            # --- END ---
            \
            -daemonize
          echo "✅ VM is running."
          echo "🌐 RDP port 3389 forwarded → VNC display on :1 (Port 5901)"
          echo "🔍 QEMU processes:"
          ps aux | grep qemu

      - name: 📋 Displaying Access Details
        run: |
          echo "Waiting 10s for ngrok tunnels to establish..."
          sleep 10
          echo "===================================================================="
          echo "✅ Your VM is installing automatically!"
          echo "   Using .github/win/autounattend.xml"
          echo ""
          echo "You can watch the automated install on VNC (port 5901)."
          echo "!! DO NOT CLICK ANYTHING IN VNC !!"
          echo ""
          echo "After ~20-30 minutes, the VM will be ready."
          echo "Connect via RDP (port 3389)."
          echo "Credentials are set in your autounattend.xml file."
          echo ""
          echo "Find your ngrok addresses on your dashboard:"
          echo "https://dashboard.ngrok.com/cloud-edge/endpoints"
          echo "===================================================================="

      - name: ⏳ Keeping VM Active (6 Hours, Minsk Time)
        run: |
          echo "🕐 Your virtual workspace will remain active until:"
          TZ="Europe/Minsk" date -d "+6 hours" +"%Y-%m-%d %H:%M:%S %Z"
          echo ""
          echo "🕒 Keeping your virtual workspace alive for 6 hours..."
          for i in $(seq 1 360); do
            printf "\r⏳ Runtime: %02d / 360 minutes elapsed" "$i"
            sleep 60
          done
          echo -e "\n💤 Session complete — shutting down."
